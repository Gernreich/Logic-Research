sed 's/,/	/g' $1|while read a; read b;do echo "$a\n$b"|datamash transpose;done|awk ' {split( $0, a, " " ); asort( a ); for( i = 1; i <= length(a); i++ ) printf( "%s ", a[i] ); printf( "\n" ); }'|sed 's/ /	/g'|while read a; read b;read c;do echo "$a\n$b\n$c"|datamash transpose;done|while read a; read b;do echo "$a\n$b"|datamash transpose|sort -n|datamash transpose;done|sed 's/	/,/g'
